# Enforce the node/npm versions declared in package.json's "engines" block, rather than
# warning and carrying on. Keeps the toolchain requirement in one place: npm itself checks
# it, so the build scripts do not have to.
engine-strict=true
